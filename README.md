# subprocess-cpp

Simple subprocess library for C++

## Description

Allows to start a child process easily.
Allows to monitor or capture its standard output and error streams.
Allows to save output into the log file.
It is based on the C library [subprocess.h](https://github.com/sheredom/subprocess.h).
